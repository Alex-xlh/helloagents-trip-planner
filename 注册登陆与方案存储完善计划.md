# 注册登陆 & 方案存储 — 逻辑完善计划书

> **项目名称：** HelloAgents Trip Planner  
> **审计日期：** 2026-06-24  
> **文档版本：** v1.0  
> **涉及模块：** 认证系统（注册/登录/JWT）、行程历史存储  
> **预计工期：** 3-4 个工作日（视实施优先级而定）

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [当前架构速览](#2-当前架构速览)
3. [问题清单（按优先级分级）](#3-问题清单按优先级分级)
4. [详细实施方案](#4-详细实施方案)
   - [阶段一：安全修复（P0 — 必须修复）](#阶段一安全修复p0必须修复)
   - [阶段二：认证完善（P1 — 强烈建议）](#阶段二认证完善p1强烈建议)
   - [阶段三：行程存储完善（P2 — 功能补全）](#阶段三行程存储完善p2功能补全)
   - [阶段四：代码质量（P3 — 技术债务）](#阶段四代码质量p3技术债务)
5. [依赖变更清单](#5-依赖变更清单)
6. [完整文件变更清单](#6-完整文件变更清单)
7. [验收标准](#7-验收标准)
8. [注意事项](#8-注意事项)

---

## 1. 背景与目标

### 1.1 当前状态

项目目前实现了一套基础的用户认证 + 行程存储系统：

- **认证**：用户名+密码注册/登录，JWT（HS256）鉴权，7 天过期
- **存储**：登录用户生成旅行计划后自动存档到 SQLite，可在历史页查看/加载

### 1.2 审计结论

经全面代码审计，发现 **15 个问题**，涉及安全漏洞、功能缺口和技术债务三个维度。其中存在 **3 个安全级别问题**需要优先修复（硬编码密钥、无密码校验、无速率限制）。

### 1.3 改造目标

- **安全加固**：消除硬编码密钥、增加暴力破解防护、加强密码策略
- **体验完善**：注册自动登录、密码确认、删除行程、分页浏览
- **代码规范化**：标准 Pinia store、显式依赖声明、数据校验

---

## 2. 当前架构速览

### 2.1 项目结构（仅相关文件）

```
backend/
├── .env                              # 环境变量（需新增 JWT_SECRET_KEY）
├── requirements.txt                  # Python 依赖（需补充 bcrypt/jose/slowapi）
├── app/
│   ├── config.py                     # Pydantic Settings 配置类
│   ├── api/
│   │   ├── main.py                   # FastAPI 应用 + 生命周期 + CORS
│   │   └── routes/
│   │       ├── auth.py               # POST /register, POST /login, GET /me
│   │       └── history.py            # POST /, GET /, GET /{id}
│   ├── core/
│   │   ├── auth.py                   # JWT 签发/校验、bcrypt、Depends 注入
│   │   └── database.py              # SQLite 异步引擎 + get_db 依赖
│   └── models/
│       ├── db.py                     # ORM: User, TripHistory
│       └── schemas.py               # Pydantic 模型: TripPlan, DayPlan, etc.

frontend/
├── package.json                      # 需新增 pinia 依赖
├── src/
│   ├── main.ts                       # Vue 入口（需注册 Pinia）
│   ├── store/
│   │   └── auth.ts                   # 当前非标准 auth store（将被替换）
│   ├── services/
│   │   └── api.ts                    # Axios 实例 + 拦截器 + 流式请求
│   ├── components/
│   │   └── NavBar.vue                # 导航栏 + 登录/注册弹窗
│   └── views/
│       ├── Home.vue                  # 首页：表单 + 流式生成 + 自动存档
│       ├── History.vue               # 历史页：列表 + 点击加载
│       └── Result.vue                # 结果页：展示/编辑/导出旅行计划
```

### 2.2 认证流程

```
注册: POST /api/auth/register {username, password}
  → bcrypt 哈希密码 → 存入 users 表 → 返回 {id, username}

登录: POST /api/auth/login {username, password}
  → 验证 bcrypt → 签发 HS256 JWT (sub=username, exp=now+7d)
  → 返回 {access_token, token_type: "bearer"}

鉴权: 请求头 Authorization: Bearer <token>
  → get_current_user_optional 解码 JWT → 查询 users 表 → 注入 User 对象
  → get_current_user (在其之上) 要求非空，否则 401
```

### 2.3 存储流程

```
生成计划: POST /api/trip/plan/stream (无需认证)
  → LLM 流式返回 → 前端解析 TripPlan JSON
  → 写入 sessionStorage（用于 Result 页面展示）
  → 若已登录: POST /api/history {destination, trip_data} 自动存档

查看历史: GET /api/history (需认证)
  → 返回当前用户的所有行程（仅摘要，不含 trip_data）

加载详情: GET /api/history/{id} (需认证，校验归属)
  → 返回完整 trip_data → 前端写入 sessionStorage → 跳转 Result 页面
```

---

## 3. 问题清单（按优先级分级）

### 🔴 P0 — 安全漏洞（必须修复，预计 0.5 天）

| # | 问题 | 风险等级 | 文件位置 |
|---|------|---------|---------|
| 1 | **JWT SECRET_KEY 硬编码** | 🔴 高 | `backend/app/core/auth.py:21` — `SECRET_KEY = "super-secret-key-for-trip-planner-auth"` |
| 2 | **无密码强度验证** | 🟡 中 | 后端 `routes/auth.py:19` UserCreate、前端 `NavBar.vue:48` |
| 3 | **无速率限制** | 🟡 中 | `/api/auth/login`、`/api/auth/register` 无任何频率限制 |

### 🟡 P1 — 认证功能缺陷（建议修复，预计 0.5 天）

| # | 问题 | 位置 |
|---|------|------|
| 4 | 注册无"确认密码"字段 | 前端 `NavBar.vue` 仅有 `password` 输入框 |
| 5 | 注册成功后需手动切回登录 | `NavBar.vue:118-121` 注册成功仅提示，不自动登录 |
| 6 | 前端无法感知 Token 过期 | `auth.ts` 不解析 JWT 的 `exp` 字段，401 才被动退出 |
| 7 | `datetime.utcnow()` 已弃用 | `auth.py:41-42`、`db.py:14,25` — Python 3.12+ 应使用 `datetime.now(datetime.UTC)` |

### 🟢 P2 — 行程存储功能缺口（建议修复，预计 1 天）

| # | 问题 | 位置 |
|---|------|------|
| 8 | **没有删除端点** | `routes/history.py` 仅有 POST 和 GET，无 DELETE |
| 9 | **没有分页** | `GET /api/history` 一次性返回所有记录 |
| 10 | **重复保存无检测** | 每次生成都创建新记录，可刷出大量重复 |
| 11 | **自动存档失败静默** | `Home.vue:315-322` 仅 `console.error`，用户无感知 |
| 12 | 历史列表信息贫瘠 | 卡片仅显示目的地+日期，无天数/预算等预览 |
| 13 | sessionStorage 依赖脆弱 | Result 页面刷新即丢失数据，无回退机制 |

### 🔵 P3 — 代码质量 / 技术债务（可选修复，预计 0.5 天）

| # | 问题 | 位置 |
|---|------|------|
| 14 | Auth store 非标准实现 | `frontend/src/store/auth.ts` 用 reactive() 组合函数模拟，非 Pinia |
| 15 | trip_data 无 Schema 校验 | `history.py:16` 用 `dict` 类型，保存时不验证是否为合法 TripPlan |
| — | 隐式依赖未声明 | `requirements.txt` 缺少 `bcrypt`、`python-jose`（auth.py 实际使用） |

---

## 4. 详细实施方案

---

### 阶段一：安全修复（P0 — 必须修复）

---

#### 修复 1：JWT SECRET_KEY 环境变量化

**现状：**
```python
# backend/app/core/auth.py:21
SECRET_KEY = "super-secret-key-for-trip-planner-auth"  # 硬编码！
```

**目标：** 密钥从 `.env` 读取，不写入代码仓库。

**实施步骤：**

**步骤 1.1** — `backend/app/config.py` Settings 类新增字段：

```python
# 在 log_level 前添加
jwt_secret_key: str = "change-me-in-production"  # 从 .env 读取
```

**步骤 1.2** — `backend/app/core/auth.py` 改为从配置读取：

```python
# 删除原有的硬编码行，改为：
from ..config import get_settings
settings = get_settings()
SECRET_KEY = settings.jwt_secret_key
```

同时新增一个辅助函数 `decode_access_token()`，供前端 token 过期检测使用（见修复 6）：

```python
def decode_access_token(token: str) -> Optional[dict]:
    """解码 JWT token，返回 payload 或 None"""
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        return payload
    except JWTError:
        return None
```

**步骤 1.3** — `backend/.env` 添加配置项（生成强随机密钥）：

```bash
# 用 python -c "import secrets; print(secrets.token_urlsafe(32))" 生成
JWT_SECRET_KEY=nxj9J1uINIkC1gKFhv20P1Ir8yRS14eRsXP15GQkyoY
```

> ⚠️ **安全性说明：** 此为示例密钥。生产部署时应使用独立生成的随机密钥，且不应提交到版本控制。建议使用 `openssl rand -hex 32` 或 `python -c "import secrets; print(secrets.token_urlsafe(32))"` 生成。

---

#### 修复 2：密码强度验证

**后端验证** — `backend/app/api/routes/auth.py` 的 `UserCreate` 添加 validator：

```python
from pydantic import BaseModel, field_validator

class UserCreate(BaseModel):
    username: str
    password: str

    @field_validator('password')
    @classmethod
    def validate_password_strength(cls, v: str) -> str:
        if len(v) < 6:
            raise ValueError('密码长度不能少于6位')
        if v.isdigit():
            raise ValueError('密码不能为纯数字')
        # 可选增强：检查是否包含字母
        # if not any(c.isalpha() for c in v):
        #     raise ValueError('密码必须包含至少一个字母')
        return v

    @field_validator('username')
    @classmethod
    def validate_username(cls, v: str) -> str:
        if len(v.strip()) < 2:
            raise ValueError('用户名长度不能少于2位')
        if len(v) > 50:
            raise ValueError('用户名长度不能超过50位')
        return v.strip()
```

**前端验证** — `frontend/src/components/NavBar.vue` 注册表单的 password 规则改为：

```vue
<a-form-item
  label="密码"
  name="password"
  :rules="[
    { required: true, message: '请输入密码' },
    { min: 6, message: '密码至少6位' }
  ]"
>
```

> 💡 注意：前端验证是 UX 层面的即时反馈，后端验证才是安全保障线，两者都需要。

---

#### 修复 3：速率限制（Rate Limiting）

**依赖：** `slowapi`（已安装在阶段 0 的依赖补充中）

**步骤 3.1** — `backend/app/api/main.py` 添加 Limiter 中间件：

```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address
from slowapi.errors import RateLimitExceeded

# 在 app = FastAPI(...) 之前
limiter = Limiter(key_func=get_remote_address, default_limits=["200/hour"])

# 在 app = FastAPI(...) 之后、app.add_middleware 之前
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
```

**步骤 3.2** — `backend/app/api/routes/auth.py` 对敏感端点限流：

```python
from slowapi import Limiter
from slowapi.util import get_remote_address
from ..main import limiter  # 或直接在 main.py 中 export limiter

# 在 register 和 login 函数上添加装饰器
@router.post("/register", response_model=UserResponse)
@limiter.limit("5/minute")
async def register(user: UserCreate, db: AsyncSession = Depends(get_db)):
    ...

@router.post("/login", response_model=Token)
@limiter.limit("5/minute")
async def login(user_credentials: UserLogin, db: AsyncSession = Depends(get_db)):
    ...
```

> 💡 `limiter` 对象需要在 `main.py` 中创建并 export，供 routes 模块引用。参考 [slowapi 文档](https://slowapi.readthedocs.io/)。

---

### 阶段二：认证完善（P1 — 强烈建议）

---

#### 修复 4：注册密码确认

**文件：** `frontend/src/components/NavBar.vue`

**改动：**

1. 新增 `confirmPassword` 响应式变量
2. 注册表单中，密码字段后新增确认密码输入框（仅在 `!isLoginMode` 时显示）
3. 添加校验规则：`{ validator: (rule, value) => value === formState.password, message: '两次密码不一致' }`
4. 切换登录/注册模式时清空密码字段

**关键代码片段：**

```vue
<!-- 确认密码字段：仅在注册模式下显示 -->
<a-form-item
  v-if="!isLoginMode"
  label="确认密码"
  name="confirmPassword"
  :rules="[
    { required: true, message: '请再次输入密码' },
    { validator: validateConfirmPassword, trigger: 'change' }
  ]"
>
  <a-input-password v-model:value="formState.confirmPassword" placeholder="再次输入密码" size="large" />
</a-form-item>
```

```typescript
// script 中新增
const formState = reactive({
  username: '',
  password: '',
  confirmPassword: ''  // 新增
})

const validateConfirmPassword = (_rule: any, value: string) => {
  if (value && value !== formState.password) {
    return Promise.reject('两次输入的密码不一致')
  }
  return Promise.resolve()
}
```

---

#### 修复 5：注册后自动登录

**方案：后端 register 直接返回 Token**（推荐，一次请求完成）

**后端** — `backend/app/api/routes/auth.py`：

```python
# /register 的 response_model 改为 Token，注册成功后直接创建 token 返回
@router.post("/register", response_model=Token)
@limiter.limit("5/minute")
async def register(user: UserCreate, db: AsyncSession = Depends(get_db)):
    # 检查用户名是否已存在（同上）
    stmt = select(User).where(User.username == user.username)
    result = await db.execute(stmt)
    if result.scalars().first():
        raise HTTPException(status_code=400, detail="用户名已存在")

    # 创建新用户
    hashed_password = get_password_hash(user.password)
    db_user = User(username=user.username, password_hash=hashed_password)
    db.add(db_user)
    await db.commit()
    await db.refresh(db_user)

    # 注册即登录：直接签发 token
    access_token_expires = timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    access_token = create_access_token(
        data={"sub": db_user.username}, expires_delta=access_token_expires
    )
    return {"access_token": access_token, "token_type": "bearer"}
```

**前端** — `frontend/src/components/NavBar.vue` 的 `onFinish`：

```typescript
const onFinish = async () => {
  loading.value = true
  try {
    const endpoint = isLoginMode.value ? '/api/auth/login' : '/api/auth/register'
    const response = await apiClient.post(endpoint, {
      username: formState.username,
      password: formState.password
    })

    // 注册和登录现在都返回 {access_token, token_type}
    const { access_token } = response.data
    authStore.login(access_token, formState.username)
    message.success(isLoginMode.value ? '登录成功' : '注册成功，已自动登录')
    showLogin.value = false
  } catch (error: any) {
    // 错误处理逻辑不变
  } finally {
    loading.value = false
  }
}
```

---

#### 修复 6：Token 过期感知

**文件：** `frontend/src/store/auth.ts`（或改造后的 Pinia store）

**改动：** 在存储 token 时解析 JWT payload，记录过期时间：

```typescript
interface TokenPayload {
  sub: string      // username
  exp: number      // 过期时间戳 (Unix timestamp in seconds)
  iat?: number     // 签发时间
}

function parseJwtPayload(token: string): TokenPayload | null {
  try {
    const base64Url = token.split('.')[1]
    const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/')
    const jsonPayload = decodeURIComponent(
      atob(base64).split('').map(c => '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2)).join('')
    )
    return JSON.parse(jsonPayload)
  } catch {
    return null
  }
}

function isTokenExpired(token: string): boolean {
  const payload = parseJwtPayload(token)
  if (!payload || !payload.exp) return true
  // 提前 5 分钟视为过期（容错）
  return (payload.exp * 1000) < (Date.now() + 5 * 60 * 1000)
}
```

在请求拦截器中增加过期检查：

```typescript
// api.ts 请求拦截器
apiClient.interceptors.request.use((config) => {
  const authStore = useAuthStore()
  if (authStore.token) {
    if (isTokenExpired(authStore.token)) {
      authStore.logout()
      message.warning('登录已过期，请重新登录')
      return Promise.reject(new Error('Token expired'))
    }
    config.headers['Authorization'] = `Bearer ${authStore.token}`
  }
  return config
})
```

---

#### 修复 7：datetime 现代化

**涉及文件：**

`backend/app/core/auth.py` 第 41-42 行：
```python
# 改前
expire = datetime.utcnow() + expires_delta
expire = datetime.utcnow() + timedelta(minutes=15)

# 改后
expire = datetime.now(timezone.utc) + expires_delta
expire = datetime.now(timezone.utc) + timedelta(minutes=15)
```

> 注意需要在文件头部 `from datetime import datetime, timedelta, timezone`

`backend/app/models/db.py` 第 14、25 行：
```python
# 改前
created_at = Column(DateTime, default=datetime.utcnow)

# 改后
from datetime import datetime, timezone
created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))
```

> ⚠️ 注意：`User` 和 `TripHistory` 两处都需要改。使用 `lambda` 避免在模块加载时固化时间。

---

### 阶段三：行程存储完善（P2 — 功能补全）

---

#### 修复 8：新增删除行程端点

**后端** — `backend/app/api/routes/history.py` 新增：

```python
from fastapi import HTTPException

@router.delete("/{trip_id}")
async def delete_trip(
    trip_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """删除当前用户的指定行程"""
    stmt = select(TripHistory).where(
        TripHistory.id == trip_id,
        TripHistory.user_id == current_user.id
    )
    result = await db.execute(stmt)
    trip = result.scalars().first()

    if not trip:
        raise HTTPException(status_code=404, detail="历史行程未找到")

    await db.delete(trip)
    await db.commit()
    return {"message": "已删除", "id": trip_id}
```

**前端** — `frontend/src/views/History.vue` 每个卡片添加删除按钮：

```vue
<div class="trip-card" @click="viewTrip(trip.id)">
  <!-- 删除按钮：阻止事件冒泡 -->
  <a-popconfirm
    title="确定删除此行程？"
    ok-text="删除"
    cancel-text="取消"
    @confirm="deleteTrip(trip.id)"
    @click.stop
  >
    <button class="delete-btn" @click.stop>✕</button>
  </a-popconfirm>
  <!-- 卡片内容 -->
</div>
```

```typescript
const deleteTrip = async (id: number) => {
  try {
    await apiClient.delete(`/api/history/${id}`)
    message.success('已删除')
    fetchTrips()  // 重新加载列表
  } catch (error) {
    message.error('删除失败')
  }
}
```

---

#### 修复 9：分页

**后端** — `GET /api/history` 改为分页响应：

```python
from sqlalchemy import func

class PaginatedTripResponse(BaseModel):
    items: List[TripHistoryResponse]
    total: int
    page: int
    page_size: int
    total_pages: int

@router.get("", response_model=PaginatedTripResponse)
async def list_trips(
    page: int = 1,
    page_size: int = 20,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    if page < 1:
        page = 1
    if page_size < 1 or page_size > 100:
        page_size = 20

    # 查询总数
    count_stmt = select(func.count()).select_from(TripHistory).where(
        TripHistory.user_id == current_user.id
    )
    total_result = await db.execute(count_stmt)
    total = total_result.scalar() or 0

    # 查询当前页
    stmt = (
        select(TripHistory)
        .where(TripHistory.user_id == current_user.id)
        .order_by(TripHistory.created_at.desc())
        .offset((page - 1) * page_size)
        .limit(page_size)
    )
    result = await db.execute(stmt)
    trips = result.scalars().all()

    return {
        "items": trips,
        "total": total,
        "page": page,
        "page_size": page_size,
        "total_pages": max(1, (total + page_size - 1) // page_size)
    }
```

**前端** — `History.vue` 底部添加分页器：

```vue
<div class="pagination-wrapper" v-if="total > pageSize">
  <a-pagination
    v-model:current="currentPage"
    :total="total"
    :page-size="pageSize"
    @change="fetchTrips"
    show-size-changer
  />
</div>
```

```typescript
const currentPage = ref(1)
const pageSize = ref(20)
const total = ref(0)

const fetchTrips = async () => {
  loading.value = true
  try {
    const response = await apiClient.get('/api/history', {
      params: { page: currentPage.value, page_size: pageSize.value }
    })
    trips.value = response.data.items
    total.value = response.data.total
  } catch (error: any) {
    message.error('获取历史记录失败')
  } finally {
    loading.value = false
  }
}
```

---

#### 修复 10：重复保存检测

**思路：** 保存前检查是否存在相同 `(user_id, destination, trip_hash)` 的记录。`trip_hash` 是 `trip_data` JSON 的 MD5 值。若已存在则跳过创建（幂等返回已有记录）。

**步骤 10.1** — `backend/app/models/db.py` 的 `TripHistory` 新增字段：

```python
from sqlalchemy import String

class TripHistory(Base):
    # ... 现有字段保持不变 ...
    trip_hash = Column(String(64), nullable=True)  # MD5 hash of trip_data JSON
```

> ⚠️ 新增字段后，首次启动应用需重建表（当前无迁移系统，可在启动时 `create_all` 自动添加列）。生产环境建议引入 Alembic 管理变更。

**步骤 10.2** — `backend/app/api/routes/history.py` 的 `save_trip` 改动：

```python
import hashlib
import json

@router.post("", response_model=TripHistoryResponse)
async def save_trip(
    trip: TripHistoryCreate,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    # 计算 trip_data 的 hash
    trip_json = json.dumps(trip.trip_data, sort_keys=True, ensure_ascii=False)
    trip_hash = hashlib.md5(trip_json.encode('utf-8')).hexdigest()

    # 检查是否已存在完全相同的行程
    stmt = select(TripHistory).where(
        TripHistory.user_id == current_user.id,
        TripHistory.destination == trip.destination,
        TripHistory.trip_hash == trip_hash
    )
    result = await db.execute(stmt)
    existing = result.scalars().first()

    if existing:
        # 更新 created_at 为最新（"重新保存"语义）
        existing.created_at = datetime.now(timezone.utc)
        await db.commit()
        return existing

    # 创建新记录
    db_trip = TripHistory(
        user_id=current_user.id,
        destination=trip.destination,
        trip_data=trip.trip_data,
        trip_hash=trip_hash
    )
    db.add(db_trip)
    await db.commit()
    await db.refresh(db_trip)
    return db_trip
```

> 💡 使用 `sort_keys=True` 确保相同内容不同 key 顺序的 JSON 也生成相同 hash。

---

#### 修复 11：自动存档错误提示

**文件：** `frontend/src/views/Home.vue` 第 315-322 行

```typescript
// 改前（仅静默 console.error）
} catch (e) {
  console.error('自动保存行程失败', e)
}

// 改后（用户可见提示）
} catch (e) {
  console.error('自动保存行程失败', e)
  message.warning('行程已生成，但自动存档失败。您可在"历史行程"页面手动保存')
}
```

---

#### 修复 12：历史列表丰富信息

**方案：** 列表接口返回摘要字段（不改数据库结构，在序列化时从 `trip_data` JSON 提取）

**后端** — `TripHistoryResponse` 增加计算字段：

```python
class TripHistoryResponse(BaseModel):
    id: int
    destination: str
    created_at: datetime
    travel_days: int = 0
    total_budget: int = 0
    thumbnail: str = ""

    model_config = {"from_attributes": True}

    @field_validator('travel_days', 'total_budget', 'thumbnail', mode='before')
    @classmethod
    def extract_from_trip_data(cls, v, info):
        """若值为默认值 0/空，尝试从 trip_data 中提取"""
        # 这里 info.data 可能包含 trip_data
        return v
```

更简单的方式 —— 在路由函数中手动构造响应：

```python
@router.get("", response_model=PaginatedTripResponse)
async def list_trips(...):
    # ... 查询逻辑 ...
    items = []
    for trip in trips:
        trip_data = trip.trip_data or {}
        items.append(TripHistoryResponse(
            id=trip.id,
            destination=trip.destination,
            created_at=trip.created_at,
            travel_days=len(trip_data.get('days', [])),
            total_budget=trip_data.get('budget', {}).get('total', 0) if trip_data.get('budget') else 0,
            thumbnail=trip_data.get('days', [{}])[0].get('attractions', [{}])[0].get('image_url', '') if trip_data.get('days') else ''
        ))
    return { "items": items, ... }
```

**前端** — `History.vue` 卡片使用新字段：

```vue
<div class="trip-meta">
  <span>📅 {{ new Date(trip.created_at).toLocaleDateString() }}</span>
  <span>📆 {{ trip.travel_days }}天</span>
  <span v-if="trip.total_budget">💰 ¥{{ trip.total_budget }}</span>
</div>
```

---

#### 修复 13：sessionStorage 数据传递改善

**文件：** `frontend/src/views/Result.vue`

**改动：** `onMounted` 时若 sessionStorage 无数据，显示友好回退：

```typescript
onMounted(() => {
  const stored = sessionStorage.getItem('tripPlan')
  if (!stored) {
    // 数据丢失（刷新或直接访问）
    message.warning('未找到行程数据，请重新生成或从历史记录加载')
    // 可选：提供"返回首页"和"前往历史"两个按钮
    showFallback.value = true
    return
  }
  try {
    tripPlan.value = JSON.parse(stored)
  } catch {
    message.error('行程数据损坏')
  }
})
```

模板中：

```vue
<div v-if="showFallback" class="fallback-state">
  <p>行程数据已丢失（可能是刷新页面导致）</p>
  <a-space>
    <a-button type="primary" @click="$router.push('/')">返回首页重新生成</a-button>
    <a-button @click="$router.push('/history')">从历史记录加载</a-button>
  </a-space>
</div>
```

---

### 阶段四：代码质量（P3 — 技术债务）

---

#### 修复 14：Auth Store 标准化为 Pinia

**为什么？** 当前实现每次调用 `useAuthStore()` 都创建新的 reactive wrapper，虽然在请求拦截器中能正常工作（因为共享同一个 `state` 对象引用），但非标准模式，不利于维护和测试。

**步骤 14.1** — 安装 Pinia（如未安装）：

```bash
cd frontend && npm install pinia
```

**步骤 14.2** — 新建 `frontend/src/stores/auth.ts`：

```typescript
import { defineStore } from 'pinia'

function getValidStorageItem(key: string): string {
  const item = localStorage.getItem(key)
  if (!item || item === 'null' || item === 'undefined') return ''
  return item
}

function parseJwtPayload(token: string): { exp?: number } | null {
  try {
    const base64Url = token.split('.')[1]
    const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/')
    const jsonPayload = decodeURIComponent(
      atob(base64).split('').map(c => '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2)).join('')
    )
    return JSON.parse(jsonPayload)
  } catch {
    return null
  }
}

export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: getValidStorageItem('token'),
    username: getValidStorageItem('username'),
  }),

  getters: {
    isLoggedIn: (state) => !!state.token,

    isTokenExpired: (state) => {
      if (!state.token) return true
      const payload = parseJwtPayload(state.token)
      if (!payload || !payload.exp) return true
      // 提前 5 分钟视为过期
      return (payload.exp * 1000) < (Date.now() + 5 * 60 * 1000)
    },

    userInitial: (state) => {
      return state.username ? state.username.charAt(0).toUpperCase() : ''
    },
  },

  actions: {
    login(newToken: string, newUsername: string) {
      this.token = newToken
      this.username = newUsername
      localStorage.setItem('token', newToken)
      localStorage.setItem('username', newUsername)
    },

    logout() {
      this.token = ''
      this.username = ''
      localStorage.removeItem('token')
      localStorage.removeItem('username')
    },
  },
})
```

**步骤 14.3** — `frontend/src/main.ts` 注册 Pinia：

```typescript
import { createPinia } from 'pinia'

const app = createApp(App)
const pinia = createPinia()
app.use(pinia)
app.use(router)
app.use(AntDesignVue)
app.mount('#app')
```

**步骤 14.4** — 更新所有引用 `@/store/auth` 的文件：
- `frontend/src/components/NavBar.vue` — import 改为 `@/stores/auth`
- `frontend/src/services/api.ts` — 同上
- `frontend/src/views/Home.vue` — 同上
- `frontend/src/views/History.vue` — 同上

**步骤 14.5** — 删除旧文件 `frontend/src/store/auth.ts`

---

#### 修复 15：trip_data Schema 校验

**文件：** `backend/app/api/routes/history.py`

保存前对 trip_data 进行校验（宽松模式：校验但不拒绝，只记日志）：

```python
from loguru import logger
from ...models.schemas import TripPlan

@router.post("", response_model=TripHistoryResponse)
async def save_trip(trip: TripHistoryCreate, ...):
    # 校验 trip_data 是否符合 TripPlan 结构（宽松模式）
    try:
        TripPlan.model_validate(trip.trip_data)
    except Exception as e:
        logger.warning(f"trip_data 校验未通过（仍会保存）: {e}")
        # 选择：如果要求严格，可以 raise HTTPException(400, detail=f"数据格式不正确: {e}")
    # ... 继续保存逻辑
```

> 💡 这里选择宽松模式是因为 LLM 输出有时会有细微的字段差异。如果业务需要严格校验，可改为 `raise HTTPException(400)` 拒绝保存。

---

## 5. 依赖变更清单

### 后端 (`requirements.txt`)

| 包名 | 版本 | 原因 | 类型 |
|------|------|------|------|
| `bcrypt` | `>=4.0.0` | auth.py 已使用但未声明 | 补充声明 |
| `python-jose[cryptography]` | `>=3.3.0` | auth.py 已使用但未声明 | 补充声明 |
| `slowapi` | `>=0.1.0` | 新增速率限制功能 | 新增依赖 |

### 前端 (`package.json`)

| 包名 | 版本 | 原因 | 类型 |
|------|------|------|------|
| `pinia` | `^2.x` | Vue 3 官方状态管理，替代当前自定义 store | 新增依赖 |

---

## 6. 完整文件变更清单

### 后端文件（8 个文件，3 个文件涉及）

| # | 文件 | 变更类型 | 变更内容摘要 |
|---|------|---------|-------------|
| 1 | `backend/requirements.txt` | 修改 | 添加 bcrypt、python-jose、slowapi |
| 2 | `backend/.env` | 修改 | 添加 `JWT_SECRET_KEY` |
| 3 | `backend/app/config.py` | 修改 | Settings 类新增 `jwt_secret_key` 字段 |
| 4 | `backend/app/core/auth.py` | 修改 | (1) SECRET_KEY 从配置读取 (2) 新增 decode_access_token (3) datetime 现代化 |
| 5 | `backend/app/api/main.py` | 修改 | 添加 slowapi Limiter 中间件 + limit 装饰器供路由引用 |
| 6 | `backend/app/api/routes/auth.py` | 修改 | (1) UserCreate 密码/用户名校验 (2) register 返回 Token (3) 添加速率限制装饰器 |
| 7 | `backend/app/api/routes/history.py` | 修改 | (1) 新增 DELETE 端点 (2) 分页改造 (3) 重复检测 (4) 列表摘要字段 (5) trip_data 校验 |
| 8 | `backend/app/models/db.py` | 修改 | (1) datetime 现代化 (2) TripHistory 新增 trip_hash 字段 |

### 前端文件（8 个，含新建和删除）

| # | 文件 | 变更类型 | 变更内容摘要 |
|---|------|---------|-------------|
| 1 | `frontend/package.json` | 修改 | 添加 pinia 依赖 |
| 2 | `frontend/src/main.ts` | 修改 | 注册 Pinia 插件 |
| 3 | `frontend/src/stores/auth.ts` | **新建** | Pinia 标准 store（含 token 过期检测） |
| 4 | `frontend/src/store/auth.ts` | **删除** | 旧的非标准实现，替换为 Pinia |
| 5 | `frontend/src/services/api.ts` | 修改 | (1) Pinia import 路径 (2) 请求拦截器增加 token 过期检查 |
| 6 | `frontend/src/components/NavBar.vue` | 修改 | (1) 密码确认字段 (2) 注册后自动登录 (3) Pinia import |
| 7 | `frontend/src/views/Home.vue` | 修改 | (1) 自动存档失败提示 (2) Pinia import |
| 8 | `frontend/src/views/History.vue` | 修改 | (1) 删除按钮 (2) 分页 (3) 丰富卡片信息 (4) Pinia import |
| 9 | `frontend/src/views/Result.vue` | 修改 | sessionStorage 丢失时的回退 UI |

---

## 7. 验收标准

### 7.1 安全验收

| 验收项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| JWT 密钥不硬编码 | 搜索代码中 `super-secret-key` | 无匹配 |
| 弱密码被拒绝 | `POST /api/auth/register` body `{"username":"test","password":"123"}` | 返回 400，提示密码不足 6 位 |
| 纯数字密码被拒绝 | `POST /api/auth/register` body `{"username":"test","password":"123456"}` | 返回 400，提示密码不能为纯数字 |
| 登录速率限制 | 1 分钟内连续调用 `/api/auth/login` 6 次 | 第 6 次返回 429 Too Many Requests |
| 注册速率限制 | 1 分钟内连续调用 `/api/auth/register` 6 次 | 第 6 次返回 429 |

### 7.2 认证流程验收

| 验收项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 注册后自动登录 | 在前端注册新用户 | 弹窗关闭，导航栏显示用户头像（已登录状态） |
| 密码确认保护 | 注册时输入不一致的两次密码 | 提示"两次密码不一致"，阻止提交 |
| 有效 token 可访问需认证接口 | 使用登录获取的 token 请求 `/api/auth/me` | 返回用户信息 |
| 无效/过期 token 被拦截 | 使用伪造 token 或修改系统时间 | 返回 401，前端自动登出并提示 |
| 未登录访客可正常生成计划 | 不登录，提交旅行规划表单 | 正常流式生成，跳转 Result 页面（仅不存档） |

### 7.3 存储功能验收

| 验收项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 自动存档 | 登录后生成旅行计划 | 生成成功后有 toast "已自动存档"，历史列表中出现新记录 |
| 存档失败有提示 | 手动关闭后端或断网 | 生成成功后显示 toast "自动存档失败，可在历史页面手动保存" |
| 删除行程 | 在历史页点击删除按钮 | 弹出确认框，确认后记录消失 |
| 分页正常工作 | 创建 25+ 条行程 | 列表默认显示 20 条，底部出现分页器 |
| 重复保存不产生重复 | 用相同参数生成两次计划 | 历史列表只有 1 条对应记录（updated_at 更新） |
| 历史卡片丰富信息 | 查看历史列表 | 卡片显示：目的地、日期、天数、预算金额 |
| 删除他人行程被拒绝 | 用一个用户登录，尝试删除另一个用户的 trip | 返回 404 或 403 |
| 未登录访问历史页 | 不登录直接访问 `/history` | 返回空列表或提示登录 |

### 7.4 回归验收

| 验收项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 流式生成不受影响 | 正常提交流行规划表单 | 欢迎语打字机效果正常，JSON 解析正常，跳转 Result 正常 |
| 旧 token 失效 | 升级前登录的 token，升级后使用 | 返回 401（因为密钥变了），用户重新登录后正常 |
| Result 页面刷新 | 生成计划进入 Result 后按 F5 | 显示回退 UI："未找到行程数据" + 返回首页/查看历史按钮 |
| 历史记录加载到 Result | 历史页点击某行程 | sessionStorage 写入 trip_data → Result 正常渲染 |

---

## 8. 注意事项

### 8.1 安全警示

1. **JWT Secret Key** 不应提交到版本控制。`.env` 虽然已在 `.gitignore` 中，但建议在 `.env.example` 中保留占位符并在文档中说明。
2. **速率限制** 默认值 `5/minute` 适用于开发环境。生产部署前根据实际用户量调整（可使用 `100/hour` 等更宽松的策略）。
3. **密码策略** 当前方案（6 位 + 非纯数字）是**最小安全基线**。面向生产时建议提升为 8 位 + 至少一个字母一个数字。

### 8.2 兼容性说明

1. **Token 格式不变**：升级 JWT 密钥后，所有旧 token 立即失效，所有用户需要重新登录。应在非高峰时段部署，或提前通知用户。
2. **数据库兼容**：`trip_hash` 是新字段，`Base.metadata.create_all` 会自动添加新列（但不会对现有数据回填 hash 值）。旧数据 `trip_hash` 为 NULL，不影响功能。
3. **Pinia 迁移**：是纯前端改动，不影响 API 接口。import 路径 `@/store/auth` → `@/stores/auth` 需要全局替换。

### 8.3 建议实施顺序

```
第 1 天上午：阶段一（P0 安全修复）
  ├─ 修复 1：JWT 密钥环境变量化 (30min)
  ├─ 修复 2：密码强度验证 (30min)
  └─ 修复 3：速率限制 (45min)

第 1 天下午：阶段二（P1 认证完善）
  ├─ 修复 4+5：密码确认 + 自动登录 (60min)
  ├─ 修复 6：Token 过期感知 (30min)
  └─ 修复 7：datetime 现代化 (10min)

第 2 天：阶段三（P2 行程存储）
  ├─ 修复 8+9：删除端点 + 分页 (120min)
  ├─ 修复 10+11：重复检测 + 错误提示 (60min)
  └─ 修复 12+13：丰富信息 + 数据传递改善 (60min)

第 3 天上午：阶段四（P3 代码质量）
  ├─ 修复 14：Pinia 迁移 (90min)
  └─ 修复 15：Schema 校验 + 回归测试 (60min)
```

### 8.4 后续优化建议（本次计划范围外）

- 引入 Alembic 数据库迁移管理
- 支持邮箱注册 + 密码重置
- Token 刷新机制（Refresh Token）
- HttpOnly Cookie 替代 localStorage 存储 token（防 XSS）
- 行程分享功能
- 批量导出/导入行程

---

> **文档维护者：** Claude Code (Anthropic)  
> **审计范围：** `helloagents-trip-planner` 项目的认证+存储模块  
> **最后更新：** 2026-06-24
